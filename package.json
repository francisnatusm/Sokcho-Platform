{
  "name": "sokcho-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Sokcho Smart City Intelligence Platform",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "export:feedback": "npm run export:feedback --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
